#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while (t--)
    {
        int l,i,j;
        string s;
        cin>>l;
        getline(cin,s);
        for(i = 0; i < l-2; i += 2)
        {
            for(j = 1; j < l; j++)
            {
                if(s[j] != )
            }
        }

    }
    
}