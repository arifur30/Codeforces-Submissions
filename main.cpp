#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t, n, r[5] = {0};
    scanf("%d\n", &t);
    for(int i = 0; i<n; i++)
    {
        scanf("%d", &n);
        if(n== 1)
        {
            printf("1");
            break;
        }
        for(int x = 0; x <5; x++)
        {
            scanf("%d", &r[x]);
        }
        sort(r.begin() , r.end());
        int sum[]
    }
    return 0;
}

